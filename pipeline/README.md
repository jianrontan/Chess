# pipeline/

Python (uv). Offline data processing for the Chess Explanation Engine: Lichess
puzzle parsing, feature extraction, embeddings, and the eval harness. Runs on
the homelab (or locally), never in production.

See `CLAUDE.md` in this directory for toolchain and conventions, and
`docs/ARCHITECTURE.md` at the repo root for the full system design.
